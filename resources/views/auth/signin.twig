{% extends 'templates/app.twig' %}

{% block content %}
	<div class="row">
		<div class="col s6">
			<div class="login_infos">
				<h1 class="infos__title">
					....
				</h1>
				<p class="infos__text">
					<div>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Dolorem repellat magni neque veritatis eaque, cupiditate totam eligendi, dolores culpa maiores deserunt eius, officia quaerat vel voluptatem provident perspiciatis alias quo?</div>
					<div>Voluptate explicabo cum minima tempore in fugiat architecto eligendi numquam illum blanditiis libero non iusto repellat veritatis, dolores provident possimus voluptatem. Ducimus dolorem hic rem nesciunt! Assumenda cupiditate labore repellendus!</div>
				</p>
			</div>
		</div>
		<div class="col s6">
			<form class="login__form" action="{{ path_for('auth.signin') }}" method="post">
				<div class="input-field {{ errors.email ? ' error' : '' }}">
					<label for="email">E-mail</label>
					<input type="email" name="email" class="validate" id="email" value="{{ old.email }}">
                    {% if errors.email %}
						<span class="help">{{ errors.email | first }}</span>
                    {% endif %}
				</div>

				<div class="input-field {{ errors.password ? ' error' : '' }}">
					<label for="password">Mot de passe</label>
					<input type="password" name="password" class="validate" id="password">
                    {% if errors.password %}
						<span class="help">{{ errors.password | first }}</span>
                    {% endif %}
				</div>
				<button type="submit" class="btn">Connexion</button>

                {{ csrf.field | raw }}
			</form>
		</div>
	</div>
{% endblock %}